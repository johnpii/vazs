@model vazs.server.Models.Department

@{
    ViewData["Title"] = "DeleteDepartment";
}

<div class="text-left">
    <a asp-action="Index" class="btn btn-secondary">
        <i class="bi bi-arrow-left"></i> Обратно к списку
    </a>
</div>

<h1>Удаление отдела разработки</h1>

<h3>Вы уверены что хотите удалить этот отдел разработки?</h3>
<div>
    <img src="data:image/jpeg;base64,@Convert.ToBase64String(Model.Image)" class="w-25" asp-append-version="true">
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            Название
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
        <dt class = "col-sm-2">
            Описание
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Description)
        </dd>
    </dl>
    
    <form asp-action="DeleteDepartment" asp-route-id="@ViewContext.RouteData.Values["uid"]">
        <input type="submit" value="Удалить" class="btn btn-danger" />
    </form>
</div>
